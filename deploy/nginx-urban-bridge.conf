# Rewrite bridge URL without .php (SPA was serving index.html → 404)
# Install: /www/server/panel/vhost/nginx/extension/bbnshop.in/bridge.conf
location = /api/urban-bridge {
    rewrite ^ /api/urban-bridge.php?$is_args$args? last;
}
