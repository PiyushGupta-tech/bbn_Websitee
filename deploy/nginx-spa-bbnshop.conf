# React SPA: serve index.html for routes like /login, /checkout, /admin
# Install: /www/server/panel/vhost/nginx/extension/bbnshop.in/spa.conf
# Do NOT add try_files on /api/ — PHP files under /api/*.php must use enable-php-83.conf
location / {
    try_files $uri $uri/ /index.html;
}
